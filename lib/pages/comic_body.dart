import 'package:flutter/material.dart';

class ComicBody extends StatelessWidget {
  const ComicBody({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('Search For Comics Here!!!!'),
    );
  }
}
